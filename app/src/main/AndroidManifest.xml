<?xml version="1.0" encoding="utf-8"?>
<!-- 阅读提示：应用清单：声明 Activity、权限、文件共享、网络安全配置和应用入口。 注释使用简体中文并按 UTF-8 保存，避免 Android Studio 中出现乱码。 -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DataCollector">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.DataCollector">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.project.ProjectListActivity"
            android:exported="false"
            android:theme="@style/Theme.DataCollector.NoActionBar" />

        <activity
            android:name=".ui.upload.DataUploadActivity"
            android:exported="false"
            android:theme="@style/Theme.DataCollector.NoActionBar" />

        <activity
            android:name=".ui.collection.screen.CollectionAndPlaybackActivity"
            android:exported="false"
            android:theme="@style/Theme.DataCollector.NoActionBar" />

        <activity
            android:name=".ui.diagnostic.DeviceDiagnosticActivity"
            android:exported="false"
            android:theme="@style/Theme.DataCollector.NoActionBar" />

        <activity
            android:name=".ui.log.OperationLogCenterActivity"
            android:exported="false"
            android:theme="@style/Theme.DataCollector.NoActionBar" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
